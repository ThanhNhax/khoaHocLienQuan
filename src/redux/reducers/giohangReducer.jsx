const initialState = {
  gioHang: [
    {
      maSp: 1,
      tenSp: "Iphone",
      soLuong: 1,
      giaBan: 1000,
      thanhTien: 1000,
    },
  ],
};

export default (state = initialState, { action }) => {
  //   switch (action.type) {
  // case first:
  //   return { ...state, ...payload };
  // default:
  //   return state;
  //   }
  return state;
};
